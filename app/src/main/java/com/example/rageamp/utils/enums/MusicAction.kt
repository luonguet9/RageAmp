package com.example.rageamp.utils.enums

enum class MusicAction(val action: Int, val actionName: String) {
    START(0, "START"),
    PAUSE(1, "PAUSE"),
    RESUME(2, "RESUME"),
    NEXT(3, "NEXT"),
    PREVIOUS(4, "PREVIOUS")
}
